services:
  chainlit-app:
    build: .
    container_name: chainlit-standalone
    env_file:
      - .env
    ports:
      - "8077:8000"
    environment:
      - CHAINLIT_HOST=0.0.0.0
    restart: unless-stopped
